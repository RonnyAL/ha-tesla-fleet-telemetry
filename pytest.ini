[pytest]
testpaths = tests
asyncio_mode = auto
# Put the repo root on sys.path so `custom_components.tesla_telemetry` imports.
# `python -m pytest` happens to work without this because it prepends the
# working directory, but the bare `pytest` console script (what CI runs) does
# not — the config-flow tests then fail to collect.
pythonpath = .
