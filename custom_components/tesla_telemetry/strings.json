{
  "config": {
    "step": {
      "pick_implementation": {
        "title": "Pick the Tesla developer-app credential set",
        "description": "Choose existing application credentials or add new ones. Add the same client_id / client_secret you registered for `tesla_fleet` — each integration goes through OAuth independently and gets its own refresh token, so they no longer rotate over each other."
      },
      "auth": {
        "title": "Authorize with Tesla"
      },
      "reauth_confirm": {
        "title": "Re-authorize with Tesla",
        "description": "Sign in to Tesla again for {vehicle} ({vin}).\n\nThis is needed when the stored login stops working, or after the integration starts requesting a new permission — Tesla only grants added permissions on a fresh sign-in, so existing entries keep using the ones they were created with until you re-authorize here.\n\nYour vehicle, endpoint and signal settings are kept, and the vehicle's history is preserved. Sign in with the same Tesla account."
      },
      "region": {
        "title": "Region",
        "description": "Tesla serves Fleet API accounts from regional endpoints. The region detected from your Tesla account is preselected — confirm it or correct it if needed.",
        "data": {
          "region": "Region"
        }
      },
      "vehicle": {
        "title": "Select vehicle",
        "description": "Choose the VIN this integration should track.",
        "data": {
          "vin": "Vehicle"
        }
      },
      "endpoint": {
        "title": "Telemetry endpoint",
        "description": "Configure how Tesla reaches Home Assistant. The hostname must resolve publicly and present a TLS cert valid for the chain you'll put in `fleet_telemetry_config.ca`. The proxy secret is what nginx will inject as `X-Tesla-Proxy-Secret` on every forwarded request — copy it into your nginx config.\n\nThe partner private key is the EC P-256 (secp256r1) key whose public counterpart is hosted at `https://your-partner-domain/.well-known/appspecific/com.tesla.3p.public-key.pem`. If you don't have one yet, generate it with `openssl ecparam -genkey -name prime256v1 -noout -out partner.key` and paste the file contents below.\n\nWhen adding another vehicle these fields are pre-filled from your existing setup — leave them unchanged unless your deployment moved.",
        "data": {
          "hostname": "Public hostname (e.g. tesla-telemetry.example.com)",
          "port": "Port",
          "partner_domain": "Partner domain (where .well-known is hosted)",
          "proxy_secret": "Proxy shared secret",
          "private_key_pem": "Partner private key (PEM)"
        }
      }
    },
    "error": {
      "vin_not_found": "That VIN is not on this account.",
      "bad_key": "Could not parse the private key. It must be a PEM-encoded private key.",
      "wrong_curve": "The key must be EC P-256 (curve secp256r1)."
    },
    "abort": {
      "already_configured": "This vehicle is already configured.",
      "missing_credentials": "No application credentials configured. Add them via Settings → Devices & Services → Application Credentials.",
      "missing_configuration": "Application credentials are missing. Add them and try again.",
      "no_url_available": "No URL available for the OAuth callback. Configure an external URL or my.home-assistant.io.",
      "user_rejected_authorize": "User rejected the authorize request.",
      "authorize_url_timeout": "Timed out generating the authorize URL.",
      "authorize_url_fail": "Could not generate the authorize URL.",
      "oauth_error": "Tesla returned an OAuth error.",
      "oauth_failed": "Could not complete the OAuth flow.",
      "oauth_timeout": "Timed out completing the OAuth flow.",
      "oauth_unauthorized": "Tesla rejected the OAuth flow — the developer app may not be approved for the requested scopes.",
      "oauth_unknown_error": "Unknown OAuth error.",
      "no_vehicles": "Tesla returned no vehicles for this account.",
      "cannot_connect": "Could not reach Tesla. Check network connectivity and try again.",
      "reauth_successful": "Re-authorization was successful.",
      "wrong_account": "That Tesla account does not list this vehicle. Re-authorize with the account that owns it."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Telemetry configuration",
        "description": "Choose what this vehicle streams, how often, and what it costs.",
        "menu_options": {
          "preset": "Preset",
          "category": "Browse a category",
          "signal": "Find a signal",
          "cost": "Cost and advanced",
          "finish": "Save and close"
        }
      },
      "preset": {
        "title": "Preset",
        "description": "A preset sets an interval for each of Tesla's signal categories. It retunes signals that are already enabled and never switches new ones on. Your per-signal settings always win over the preset.",
        "data": {
          "interval_preset": "Preset"
        }
      },
      "cost": {
        "title": "Cost and advanced",
        "description": "Tesla bills per data point. Your selection of {signals} signals sends at most {ceiling} a month, assuming every signal changes at every opportunity — real usage is normally far below that. At least {floor} a month is guaranteed by the resend intervals you have set, which send even when nothing changes.",
        "data": {
          "cost_per_million_signals": "Cost per million signals",
          "assume_firmware_support": "Send newer field options without waiting for the car to confirm support"
        },
        "data_description": {
          "assume_firmware_support": "Minimum delta, resend interval and included fields need recent firmware. They are normally withheld until your car proves it supports them. Only turn this on if you know your firmware version."
        }
      }
    }
  },
  "selector": {
    "region": {
      "options": {
        "na": "North America",
        "eu": "Europe"
      }
    },
    "interval_preset": {
      "options": {
        "default": "Default — the built-in intervals",
        "eco": "Eco — stream sparingly",
        "balanced": "Balanced",
        "live": "Live — fast updates while driving",
        "high_rate": "High rate (legacy) — location and speed at 1s"
      }
    }
  }
}
